create database spider;
use spider;
create table job
(
  job_id int primary key auto_increment,
  job_name varchar(100),
  job_address varchar(100),
  job_require varchar(200),
  job_company varchar(100)
);
##设置自增主键从1开始
alter table job auto_increment = 1;